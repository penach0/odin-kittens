<h1>All Kittens</h1>

<ul>
    <% @kittens.each do |kitten| %>
        <li>
            <%= kitten.name %> 
            <%= link_to "Delete", kitten_path(kitten.id), data: { turbo_method: :delete } %> 
        </li>

    <% end %>
</ul>